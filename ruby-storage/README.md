# 💎 Ruby Storage - Файловое хранилище

Современное веб-приложение для хранения и управления файлами с удобным интерфейсом.

## ✨ Возможности

- 🔐 Безопасная авторизация по логину и паролю
- 📁 Создание папок и организация файлов
- ⬆️ Загрузка файлов через форму или drag & drop
- ⬇️ Скачивание файлов
- 🗑️ Удаление файлов и папок
- 💾 Мониторинг использования дискового пространства
- 🎨 Современный и адаптивный интерфейс
- 🚀 Высокая производительность

## 🔑 Данные для входа

- **Логин:** root
- **Пароль:** DIMAzay4214

## 📋 Требования

- Ubuntu/Debian Linux (или любой другой дистрибутив)
- Python 3.8+
- Nginx
- Доменное имя (для HTTPS)

## 🚀 Быстрый деплой (рекомендуется)

### Автоматическая установка с помощью скрипта

1. Скачайте проект на сервер
2. Перейдите в директорию проекта
3. Сделайте скрипт исполняемым:
```bash
chmod +x deploy.sh
```

4. Запустите скрипт с правами root:
```bash
sudo ./deploy.sh
```

5. Следуйте инструкциям в интерактивном меню:
   - Введите доменное имя
   - Введите email для SSL сертификата
   - Укажите, есть ли у вас существующий SSL сертификат
   - Если есть - укажите пути к cert.pem и key.pem
   - Если нет - скрипт автоматически получит Let's Encrypt сертификат

Скрипт автоматически:
- Установит все зависимости
- Настроит виртуальное окружение Python
- Создаст systemd сервис
- Настроит Nginx
- Получит/установит SSL сертификат
- Запустит приложение

## 🛠️ Ручная установка

### 1. Установка зависимостей

```bash
sudo apt update
sudo apt install -y python3 python3-pip python3-venv nginx certbot python3-certbot-nginx
```

### 2. Настройка Python окружения

```bash
cd /home/nroot/Документы/ruby-storage
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Создание systemd сервиса

Создайте файл `/etc/systemd/system/ruby-storage.service`:

```ini
[Unit]
Description=Ruby Storage Application
After=network.target

[Service]
User=root
WorkingDirectory=/home/nroot/Документы/ruby-storage
Environment="PATH=/home/nroot/Документы/ruby-storage/venv/bin"
ExecStart=/home/nroot/Документы/ruby-storage/venv/bin/gunicorn --workers 4 --bind 127.0.0.1:5000 app:app

[Install]
WantedBy=multi-user.target
```

### 4. Настройка Nginx

Создайте файл `/etc/nginx/sites-available/ruby-storage`:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    client_max_body_size 16G;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/ruby-storage /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

### 5. Получение SSL сертификата

```bash
sudo certbot --nginx -d your-domain.com
```

### 6. Запуск приложения

```bash
sudo systemctl daemon-reload
sudo systemctl enable ruby-storage
sudo systemctl start ruby-storage
```

## 🌐 Деплой на разных хостингах

### VPS/Dedicated сервер (DigitalOcean, Linode, AWS EC2)

1. Используйте автоматический скрипт `deploy.sh`
2. Убедитесь, что домен направлен на IP сервера
3. Откройте порты 80 и 443

### Shared хостинг (с Python)

1. Загрузите файлы через FTP/SFTP
2. Установите зависимости: `pip install -r requirements.txt --user`
3. Настройте WSGI согласно документации хостинга
4. Укажите `app:app` как entry point

### Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]
```

Запустите:
```bash
docker build -t ruby-storage .
docker run -d -p 5000:5000 -v $(pwd)/uploads:/app/uploads ruby-storage
```

## 🔧 Управление сервисом

```bash
# Статус
sudo systemctl status ruby-storage

# Перезапуск
sudo systemctl restart ruby-storage

# Остановка
sudo systemctl stop ruby-storage

# Логи
sudo journalctl -u ruby-storage -f

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

## 📊 Мониторинг

Приложение автоматически показывает:
- Использованное дисковое пространство
- Общий объем диска
- Процент использования

## 🔒 Безопасность

### Рекомендации:

1. **Смените пароль**: Отредактируйте `app.py` и измените хеш пароля
2. **Смените secret_key**: Замените `app.secret_key` на случайную строку
3. **Настройте firewall**:
```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp
sudo ufw enable
```

4. **Регулярные обновления**:
```bash
sudo apt update && sudo apt upgrade -y
```

5. **Бэкапы**: Регулярно создавайте резервные копии папки `uploads/`

## 🐛 Устранение неполадок

### Приложение не запускается

```bash
# Проверьте логи
sudo journalctl -u ruby-storage -n 50

# Проверьте статус
sudo systemctl status ruby-storage
```

### Ошибка Nginx

```bash
# Проверьте конфигурацию
sudo nginx -t

# Проверьте логи
sudo tail -f /var/log/nginx/error.log
```

### Проблемы с загрузкой больших файлов

Отредактируйте `/etc/nginx/sites-available/ruby-storage` и увеличьте:
```nginx
client_max_body_size 100G;  # или нужный размер
```

## 📝 Лицензия

MIT License - используйте свободно!

## 💬 Поддержка

При возникновении проблем проверьте:
1. Логи приложения
2. Логи Nginx
3. Права доступа к папке uploads/
4. Доступность домена и SSL сертификата

## 🎉 Готово!

Ваше файлохранилище Ruby готово к использованию! Наслаждайтесь удобным интерфейсом и безопасным хранением файлов.
